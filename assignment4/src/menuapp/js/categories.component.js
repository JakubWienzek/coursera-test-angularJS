(function() {
    'use strict';

    //shows all available categories in the menu to the user
    angular.module('MenuApp').
    component('categories', {
        templateUrl: '',
        binding: {
            categories: '<'
        }
    });
})();